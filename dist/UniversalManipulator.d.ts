import { ManipulatorOptions, ManipulableTarget, Mode, Orientation, Pivot, SnapConfig } from './types.js';
import { Vector3 } from './math/Vector3.js';
export declare class UniversalManipulator {
    show: boolean;
    private readonly scene;
    private readonly canvas;
    private readonly container;
    private readonly primitive;
    private readonly picker;
    private readonly controller;
    private readonly solver;
    private readonly frameBuilder;
    private readonly pivotResolver;
    private readonly hud;
    private readonly snapper;
    private targets;
    private orientation;
    private pivot;
    private snapConfig;
    private sizeOptions;
    private cursor?;
    private normal?;
    private frame?;
    private pivotResult?;
    private readonly history;
    private historyIndex;
    private pendingInitial?;
    private readonly postRenderListener;
    constructor(viewerOrScene: any, options?: ManipulatorOptions);
    setTarget(target: ManipulableTarget | ManipulableTarget[]): void;
    setOrientation(orientation: Orientation): void;
    setPivot(pivot: Pivot): void;
    enable(modes: Partial<Record<Mode, boolean>>): void;
    setSnap(config: SnapConfig): void;
    setSize(radius: number, minScale: number, maxScale: number): void;
    setVisible(visible: boolean): void;
    setCursor(position: Vector3): void;
    setNormal(normal: Vector3): void;
    undo(): void;
    redo(): void;
    destroy(): void;
    private refresh;
    private captureInitialState;
    private restoreInitialState;
    private commitHistory;
    private applyMatrixMap;
    private applyDelta;
    private static resolveScene;
    private static resolveContainer;
}
